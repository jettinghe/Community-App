<?php

class PostvotenotifiesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('postvotenotifies')->truncate();

		$postvotenotifies = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('postvotenotifies')->insert($postvotenotifies);
	}

}
