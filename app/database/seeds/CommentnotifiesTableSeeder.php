<?php

class CommentnotifiesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('commentnotifies')->truncate();

		$commentnotifies = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('commentnotifies')->insert($commentnotifies);
	}

}
